//secret Hash = TFpkdkRvcnFoUnE0N0loZQ== 

/*
function HashGenerator(input) {
    let obfuscated = "";
    for (let i = 0; i < input.length; i++) {
        obfuscated += String.fromCharCode(input.charCodeAt(i) + 3); // Shift characters
    }

    let hash = btoa(obfuscated); // Base64 encode for fake hashing
    return hash;
}*/		

